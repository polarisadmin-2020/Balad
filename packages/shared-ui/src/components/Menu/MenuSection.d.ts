import { default as React } from 'react';

export interface IMenuSectionProps {
    sectionLabel?: string;
    children?: React.ReactNode;
}
declare const MenuSection: React.FC<IMenuSectionProps>;
export default MenuSection;
